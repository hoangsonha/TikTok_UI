{
    "compilerOptions": {
        "baseUrl": ".", // . is file current
        "paths": {
            "~/*": ["src/*"] // input ~ will show all file in src, support Command Prompt when input ~/ in src
        }
    }
}
